# Rust formatting configuration for rust-comm
# Uses only stable rustfmt options for compatibility

edition = "2021"
max_width = 100
tab_spaces = 4
use_small_heuristics = "Default"
reorder_imports = true
reorder_modules = true
